import { Component } from "solid-js";
/**
 * Bulk install notifications are now unified into the primary queue toast system.
 */
const BulkInstallToast: Component = () => null;

export default BulkInstallToast;
